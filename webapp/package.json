{
  "name": "pyfinalo-webapp",
  "private": true,
  "type": "module",
  "workspaces": [
    "app",
    "repl-python",
    "repl-js",
    "worker"
  ],
  "scripts": {
    "dev": "bun run --cwd app dev",
    "build": "bun run build:repls && bun run build:app && bun run build:worker",
    "build:app": "bun run --cwd app build",
    "build:repls": "bun run build:repl-python && bun run build:repl-js",
    "build:repl-python": "bun run --cwd repl-python build",
    "build:repl-js": "bun run --cwd repl-js build",
    "build:worker": "bun run --cwd worker build",
    "test": "bun run test:app && bun run test:repls",
    "test:app": "bun run --cwd app test",
    "test:repls": "bun run --cwd repl-python test && bun run --cwd repl-js test",
    "deploy": "bun run scripts/deploy.sh",
    "upload-assets": "bun run scripts/upload-r2-assets.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}